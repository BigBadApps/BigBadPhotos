# P07 — SessionsView and RunView

**Plan:** `docs/superpowers/plans/2026-08-10-photo-sessions.md` → **Task 7 (P7)**
**Spec:** `docs/superpowers/specs/2026-08-10-photo-sessions-design.md`
**Branch:** `bbaf/bbp-sessions-P07` off `bbaf/bigbadphotos-sessions`
**Depends on:** P06 (parallel with P08)

## Your job

Execute **Task 7 (P7)** of the plan, step by step, in order. The plan contains the tests to
write, the interfaces to produce, and the commit messages. Do not improvise beyond it.

## File allowlist

You may create or modify only these paths:

- `frontend/src/api/sessionsClient.js` (create)
- `frontend/src/views/SessionsView.jsx` (create)
- `frontend/src/views/RunView.jsx` (create)
- `frontend/src/hooks/useSessionRun.js` (create)
- `frontend/src/App.jsx` (modify: route registration only)
- `frontend/src/components/BottomNavBar.jsx` (modify: one nav entry)
- `frontend/src/components/GoogleDriveFolderPicker.jsx` (modify: add a Create-folder action)
- `frontend/src/store.js` (modify: session slice only)

## Do not touch

Anything not in the allowlist. In particular: `app.py` (unless listed),
`backend/session_worker.py`, `backend/scoring.py`, `backend/topaz.py`,
`backend/google_auth.py`, `requirements.txt`, `CLAUDE.md`, `AGENTS.md`,
`Procfile`, `nixpacks.toml`, `railpack.toml`, `.env`, `.github/`.

If you believe a file outside the allowlist must change, stop and say so in your
final message instead of changing it.

## Rules

- Interpreter is `.venv/bin/python` (3.14.6). Not `python3`, not `python`.
- TDD: write the failing test first, watch it fail, then implement.
- Conventional commits. Commit at the points the plan marks.
- No new runtime dependencies.
- Never push to `main`.

## Test command

```bash
cd frontend && npm run build
```

## Definition of done

- `npm run build` exits 0.
- Obsidian Lens tokens from `frontend/src/index.css` are used — no new CSS framework, no new icon set, icons come from `Icon.jsx`.
- Every control is reachable one-handed on a 390px-wide viewport.
- Preflight failures render their `fix` text, not just `ok: false`.

**You are not finished until you paste the passing output of the test command above
into your final message.** A summary of what you did is not evidence. If the tests do
not pass, say so plainly and report where you stopped.
